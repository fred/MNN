<article id="<%= @page.id %>">
  <header>
    <h1><%= @page.title %></h1>
  </header>
  <section>
    <%= @page.body.html_safe %>
  </section>
</article>


<% content_for :breadcrumb do %>
  <li>
    <%= link_to "Home", root_path, title: "Home", rel: 'home' %>
    <span class="divider">/</span>
  </li>
  <li class="active">
    <%= @page.title %>
  </li>
<% end %>