New Post at http://WorldMathaba.net
====================================

<%=h @item.title %>

<%= truncate(
      sanitize(@item.body,
        tags: %w(),
        attributes: %w()
      ),
      length: 400,
      omission: ' ... (click link to read full article)'
    )
%>

<% if @item.user %>
  <% if @item.original? %>
  Written by <%= @item.user.public_display_name %>
  <% else %>
  Submitted by <%= @item.user.public_display_name %>
  <% end %>
<% end %>

- Category: <%= @item.category_title %>

- Tags: <%= @item.tag_list %>

To see the full article, just follow this link: <%= @url %>

To disable email notifications follow this link: https://worldmathaba.net/users/edit

Have a great day!
